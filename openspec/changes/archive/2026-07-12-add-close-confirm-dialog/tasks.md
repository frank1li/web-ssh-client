## 1. Add Close Confirmation Dialog

- [x] 1.1 Add `confirm()` dialog in `_addSessionTab()` close button handler in `client/public/app.js` — show session identifier and only call `_closeSession()` if user confirms
