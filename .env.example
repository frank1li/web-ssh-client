# Server Configuration
PORT=3000
MAX_SESSIONS=10
CONNECTION_TIMEOUT=30000

# SSH Defaults
DEFAULT_SSH_PORT=22
KEEPALIVE_INTERVAL=10000
KEEPALIVE_COUNT_MAX=3

# Logging Configuration
# Log level: DEBUG, INFO, WARN, ERROR, NONE (default: INFO)
LOG_LEVEL=INFO

# Log file path (optional, default: output to terminal only)
# Example: LOG_FILE=./logs/ssh-proxy.log
# LOG_FILE=
